require 'spec_helper'

describe Boulangerie do
  it 'has a version number' do
    expect(Boulangerie::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
